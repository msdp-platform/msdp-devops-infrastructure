#!/bin/bash

# 🚀 Multi-Service Delivery Platform Manager - Shell Wrapper
# Simple wrapper for the Python platform manager

# Get the directory of this script
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Run the Python script with all arguments
python3 "$SCRIPT_DIR/platform-manager.py" "$@"
