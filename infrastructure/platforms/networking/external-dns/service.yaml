apiVersion: v1
kind: Service
metadata:
  name: external-dns
  namespace: external-dns
  labels:
    app.kubernetes.io/name: external-dns
    app.kubernetes.io/component: dns-controller
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 7979
      targetPort: 7979
      protocol: TCP
  selector:
    app.kubernetes.io/name: external-dns
