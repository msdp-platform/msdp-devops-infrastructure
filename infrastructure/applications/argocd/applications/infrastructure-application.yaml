apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: msdp-applications
  namespace: argocd
  labels:
    app.kubernetes.io/name: msdp-applications
    app.kubernetes.io/component: applications
    app.kubernetes.io/part-of: msdp-platform
spec:
  project: default
  source:
    repoURL: https://github.com/msdp-platform/msdp-devops-infrastructure
    targetRevision: HEAD
    path: infrastructure/applications
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
  revisionHistoryLimit: 10
