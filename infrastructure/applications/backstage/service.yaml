apiVersion: v1
kind: Service
metadata:
  name: backstage
  namespace: backstage
  labels:
    app.kubernetes.io/name: backstage
    app.kubernetes.io/part-of: msdp-platform
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 7007
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: backstage
