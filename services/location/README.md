# Location Service (HTTP API + Lambda)

Terraform skeleton for an HTTP API Gateway + Lambda integration.

- HTTP API (v2) with CORS
- Lambda proxy integration (placeholder artifact path)
- Stage: `dev` with auto deploy

Update `variables.tf` defaults and the `lambda_image_uri` or zip artifact path as you wire CI builds.

